#+TITLE:       Vagrant 建立备份快照
#+AUTHOR:      venmos
#+EMAIL:       venmos@fuck.gfw.es
#+DATE:        2015-07-30
#+URI:         /blog/%y/%m/%d/vagrant-shot-backup
#+KEYWORDS:    vagrant, 快照, 备份,
#+TAGS:        vps,
#+LANGUAGE:    en
#+OPTIONS:     H:3 num:nil toc:nil \n:nil ::t |:t ^:nil -:nil f:t *:t <:t
#+DESCRIPTION: vagrant, 快照, 备份,


使用Vagrant的快照功能可以很方便快速的创建当前虚拟机的一个临时备份状态，在进行重要操作时可以先创建一个快照以便在操作失误后快速恢复。

安装Vagrant快照插件：

#+begin_src

vagrant plugin install vagrant-vbox-snapshot
#+end_src


#+begin_src

$ vagrant snapshot
Usage: vagrant snapshot <command> [<args>]

Available subcommands:
     back
     delete
     go
     list
     take

For help on any individual command run `vagrant snapshot <command> -h
#+end_src

使用方法：

- 创建一个快照

#+begin_src

vagrant snapshot take "Name"
#+end_src

- 查看快照列表

#+begin_src

vagrant snapshot list
#+end_src

- 从指定快照中恢复

#+begin_src

vagrant snapshot go "Name"
#+end_src

- 删除一个快照

#+begin_src

vagrant snapshot delete "Name"
#+end_src
